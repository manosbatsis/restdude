#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../jsx-linker/bin/jsx-linker" "$@"
  ret=$?
else 
  node  "$basedir/../jsx-linker/bin/jsx-linker" "$@"
  ret=$?
fi
exit $ret
