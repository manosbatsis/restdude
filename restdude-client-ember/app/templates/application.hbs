<div class="container">
  <div class="menu">
    {{#link-to 'index'}}
      <h1 class="left">
        <em>SuperRentals</em>
      </h1>
    {{/link-to}}
    <div class="left links">
      {{#link-to 'about'}}
        About
      {{/link-to}}
      {{#link-to 'contact'}}
        Contact
      {{/link-to}}
    </div>
  </div>
  {{#if session.isAuthenticated}}
    <button {{action 'logout'}}>Logout</button>
  {{else}}
    {{#link-to 'login'}}
        Login  
     {{/link-to}}
     {{/if}}   
  <div class="body">
    {{outlet}}
  </div>
</div>

<!-- {{#link-to 'protected'}}
        Protected 
     {{/link-to}}
-->